name: Track 6G Evolution

on:
  schedule:
    - cron: '0 0 1 * *'  # Runs monthly, at midnight on the 1st
  workflow_dispatch:

jobs:
  run-script:
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Cache pip dependencies
        uses: actions/cache@v3
        with:
          path: ~/.cache/pip
          key: ${{ runner.os }}-pip-${{ hashFiles('**/requirements.txt') }}
          restore-keys: |
            ${{ runner.os }}-pip-

      - name: Cache Playwright browsers
        uses: actions/cache@v4
        with:
          path: ~/.cache/ms-playwright
          key: ${{ runner.os }}-playwright-${{ hashFiles('**/requirements.txt') }}
          restore-keys: |
            ${{ runner.os }}-playwright-

      - name: Cache article database
        uses: actions/cache@v4
        with:
          path: |
            seen_articles.json
            fetch_method_cache.json
          key: ${{ runner.os }}-articles-${{ github.run_number }}
          restore-keys: |
            ${{ runner.os }}-articles-

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.13'

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with: 
          node-version: '20'

      - run: |
          echo "Node version:"
          node -v
          echo "npm version:"
          npm -v
          echo "npx version:"
          npx --version

      - name: Install dependencies
        run: |
          pip install -r requirements.txt
          playwright install --with-deps chromium

      - name: Test Gemini API
        env:
          GOOGLE_API_KEY: ${{ secrets.GOOGLE_API_KEY }}
        run: |
          python - << 'EOF'
          import google.genai as genai
          import os

          client = genai.Client(api_key=os.getenv("GOOGLE_API_KEY"))

          try:
              response = client.models.generate_content(
                  model="gemini-3-flash-preview",
                  contents="Hello from GitHub Actions"
              )
              print("Gemini test OK:", response.text)
          except Exception as e:
              print("Gemini test FAILED:", e)
          EOF

      - name: Start MCP 3GPP Server (background)
        env: 
           HUGGINGFACE_TOKEN: ${{ secrets.HUGGINGFACE_TOKEN }}
        run: |
          echo "Starting 3gpp-mcp-charging MCP server..."
          echo "=== Starting MCP server at $(date) ===" > mcp_server.log
          
          # Run in background with detailed logging
          npx 3gpp-mcp-charging@latest serve >> mcp_server.log 2>&1 &
          MCP_PID=$!
          echo "MCP_PID=$MCP_PID" >> $GITHUB_ENV
          echo "✓ MCP server started with PID $MCP_PID"
          
          # Wait longer for first-time download + initialization
          echo "Waiting 25 seconds for server initialization..."
          sleep 25
          
          # Check if process is still alive
          if kill -0 $MCP_PID 2>/dev/null; then
            echo "✓ MCP server is running (PID $MCP_PID)"
          else
            echo "❌ MCP server crashed!  Showing logs:"
            cat mcp_server.log
            echo "⚠️ Will continue with HTTP/sample fallback"
          fi
                 
      - name: Run tracker script and save output
        env:
          GOOGLE_API_KEY: ${{ secrets.GOOGLE_API_KEY }}
        run: |
          python track_6g.py > output.log 2>&1
          echo "Script exit code: $?"
        continue-on-error: false  # Fail workflow if script fails
      
      - name: Stop MCP Server
        if: always()
        run: |
          echo "=== MCP SERVER LOGS ==="
          cat mcp_server.log || echo "No MCP log file found"
          echo "======================"
          
          if [ !  -z "$MCP_PID" ]; then
            if kill -0 $MCP_PID 2>/dev/null; then
              kill $MCP_PID || true
              echo "MCP server stopped (PID $MCP_PID)"
            else
              echo "MCP server was already stopped"
            fi
          fi
        
      - name: Build Dashboard
        run: |
          cd dashboard
          # Note: If using Vite, we would run npm install && npm run build
          # For this vanilla setup, we just ensure latest_digest.json and momentum_data.json are in the right place
          cp ../latest_digest.json . || echo "⚠️ latest_digest.json not found"
          cp ../momentum_data.json . || echo "⚠️ momentum_data.json not found, skipping"
          cp ../source_target_matrix.json . || echo "⚠️ source_target_matrix.json not found, skipping"

      - name: Verify dashboard files
        run: |
          echo "Checking dashboard files..."
          ls -la dashboard/
          test -f dashboard/latest_digest.json && echo "✓ latest_digest.json exists" || echo "✗ latest_digest.json missing"
          test -f dashboard/momentum_data.json && echo "✓ momentum_data.json exists" || echo "✗ momentum_data.json missing"
          test -f dashboard/source_target_matrix.json && echo "✓ source_target_matrix.json exists" || echo "✗ source_target_matrix.json missing"

      - name: Commit and push results
        run: |
          git config --local user.email "github-actions[bot]@users.noreply.github.com"
          git config --local user.name "github-actions[bot]"
          git add -f 6g_digest_*.md
          git add -f seen_articles.json
          git add -f fetch_method_cache.json || echo "fetch_method_cache.json not found, skipping"
          git add -f latest_digest.json
          git add -f momentum_data.json || echo "momentum_data.json not found, skipping"
          git add -f source_target_matrix.json || echo "source_target_matrix.json not found, skipping"
          git add -f dashboard/latest_digest.json || echo "dashboard/latest_digest.json not found, skipping"
          git add -f dashboard/momentum_data.json || echo "dashboard/momentum_data.json not found, skipping"
          git add -f dashboard/source_target_matrix.json || echo "dashboard/source_target_matrix.json not found, skipping"
          git add -f output.log
          # Only commit if there are actual changes
          if ! git diff --cached --quiet; then
            git commit -m "📊 Monthly 6G digest & Dashboard update - $(date +'%Y-%m-%d')"
            git push
          else
            echo "No new articles found. Skipping commit."
          fi


      - name: Deploy to GitHub Pages
        if:  success()  # Only deploy if previous steps succeeded
        uses: JamesIves/github-pages-deploy-action@v4
        with:
          folder: dashboard
          branch: gh-pages
       
